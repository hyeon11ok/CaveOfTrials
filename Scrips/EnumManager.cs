namespace EnemyEnum {
    public enum EnemyState {
        Idle,
        Move,
        Chase,
        Attack,
        Death
    }
}

namespace ItemEnum {
    public enum WeaponRank {
        Normal,
        Unique,
        Legend
    }
}